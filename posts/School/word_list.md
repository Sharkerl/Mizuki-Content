---
title: Word List
Description: To record the words that are not familiar
published: 2026-08-07
date: 2026-08-07
draft: false
tags: ["Study", "Senior High"]
category: "School"
pinned: false
auther: Sharkerl
location: 32.08, 118.76
---

`inspiration` 好主意, 灵感;<br>
`access` 使用, 访问 v.; 机会, 通道 n.<br> 
`detect` 发现, 查明 v.;<br>
`assistant` 助理;<br>
`insight` 洞察力, 了解 n.;<br>
`deserve` 值得 v.;<br>
`deliable` 可靠的;<br>
`livelihood` 生计;<br>
`analysis` 分析 [əˈnæləsɪs];<br>
`prediction` 预告, 预测;<br>
`demand` 需求 n.;<br>
`supply` 补给 n.;<br>
`inform` 通知, 了解, 影响 v.;<br>
`trend` 趋势 n.;<br>
`track` 轨道, 踪迹 n.; 跟踪 v.;<br>
`recur` 再发生, 重现v.;<br>
`recurring` 反复的, 周期的;<br>
`monitor` 监视, 检查 v.; 监视器 n.;<br>
`issue` 问题, 期(周期的期) n.;<br>
`sustaintable` 可持续的;<br>
`enthusiast` 热心者 [ɪnˈθjuːziæst];<br>
`gap` 缺口, 间隙;<br>
`prioritize` 优先处理;<br>
`workforce` 全体员工, 劳动力;<br>
`enthusiatic` 热情的;<br>
`eager` 热情的;<br>
`complicated` 复杂的;<br>
`assign` 分配;<br>
`hesitate` 犹豫;<br>
`previous` 以往的;<br>
`typical` 典型的;<br>
`flexible` 灵活的, 柔韧的;<br>
`confuse` 混淆, 迷惑 v.;<br>
`appointment` 约定;<br>
`interaction` 相互影响;<br>
`arrangement` 安排;<br>
`cooperation` 合作, 配合;<br>

